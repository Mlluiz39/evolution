# -----------------------------
# EvolutionAPI - Configuração
# -----------------------------

# Porta (Render define automaticamente, mas você pode definir localmente)
PORT=10000

# Chave da EvolutionAPI (gerada pelo seu painel Evolution)
EVOLUTION_API_KEY=your_evolution_api_key_here

# Modo de execução (development / production)
NODE_ENV=production

# -----------------------------
# Banco de dados (PostgreSQL)
# -----------------------------
DATABASE_URL=postgres://USER:PASSWORD@HOST:PORT/DB_NAME
DATABASE_SCHEMA=public

# -----------------------------
# Redis (opcional, se você usar cache)
# -----------------------------
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# -----------------------------
# Logs / monitoramento
# -----------------------------
LOG_LEVEL=info

# -----------------------------
# API externa (exemplo WhatsApp, N8N ou outras integrações)
# -----------------------------
WHATSAPP_PROVIDER=baileys
N8N_URL=https://your-n8n-url.com
N8N_API_KEY=your_n8n_api_key_here

# -----------------------------
# Evolução do cache multi-provider
# -----------------------------
CACHE_PROVIDER=local
CACHE_TTL=3600

# -----------------------------
# Outras variáveis de integração
# -----------------------------
# Por exemplo: S3 / MinIO / Cloud Storage
S3_ENDPOINT=
S3_BUCKET=
S3_ACCESS_KEY=
S3_SECRET_KEY=
S3_REGION=

# -----------------------------
# E-mail (opcional)
# -----------------------------
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
